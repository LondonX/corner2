<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_otheruser_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".OthersUserInfoActivity">

    <include layout="@layout/toolbar_root"/>

    <ListView
        android:id="@+id/activity_otheruser_lv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@null"
        android:listSelector="@null"
        android:scrollbarThumbVertical="@drawable/shape_scrollbar"
        tools:listheader="@layout/head_user"
        tools:listitem="@layout/item_topics">
    </ListView>

</LinearLayout>